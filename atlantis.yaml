version: 2
projects:

- dir: .
  terraform_version: v0.11.7
  workflow: convert-tf
workflows:
  convert-tf:
    plan:
      steps:
        - run: ops clusters/atlantis-cluster.yaml terraform template > ./atlantis-cluster.tf
        - plan
