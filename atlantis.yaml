version: 2
projects:

- dir: .
  autoplan:
    enabled: false
  terraform_version: v0.11.7
  workflow: convert-tf
workflows:
  convert-tf:
    plan:
      steps:
        - run: ops clusters/WORKSPACE.yaml terraform plan
